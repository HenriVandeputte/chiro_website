extends layout

block content
    h1 #{title}

    form(method='POST' action='')
        div
            label(for='name') Lid:
            input#name(type='text', name='name')
        button(type='submit') Submit

    if errors
        ul
            for error in errors
                li!= error.msg
